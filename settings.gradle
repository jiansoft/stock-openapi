pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            url = uri('https://repo.spring.io/milestone')
        }
    }
}

rootProject.name = 'stock_api'
