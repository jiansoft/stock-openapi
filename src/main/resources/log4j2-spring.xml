<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="60">
    <Properties>
        <Property name="applicationName">${spring:spring.application.name:-stock_api}</Property>
        <Property name="logDirectory">${spring:logging.file.path:-${sys:LOG_DIR:-logs}}</Property>
        <Property name="archivePattern">${logDirectory}/archive/${applicationName}-%d{yyyy-MM-dd}.log</Property>
        <Property name="archiveRetention">P7D</Property>
        <Property name="logPattern">%d{yyyy-MM-dd HH:mm:ss.SSS XXX} %-5level [%t] %c{1.} - %msg%n%throwable</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${logPattern}"/>
        </Console>

        <RollingFile
            name="File"
            fileName="${logDirectory}/${applicationName}.log"
            filePattern="${archivePattern}"
        >
            <PatternLayout pattern="${logPattern}"/>
            <Policies>
                <CronTriggeringPolicy schedule="0 0 0 * * ?" evaluateOnStartup="true"/>
            </Policies>
            <DefaultRolloverStrategy>
                <Delete basePath="${logDirectory}/archive" maxDepth="1">
                    <IfFileName glob="${applicationName}-*.log"/>
                    <IfLastModified age="${archiveRetention}"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Root>
    </Loggers>
</Configuration>
